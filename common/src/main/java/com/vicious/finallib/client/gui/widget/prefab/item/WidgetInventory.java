package com.vicious.finallib.client.gui.widget.prefab.item;

import com.vicious.finallib.client.gui.widget.CompoundWidget;
import net.minecraft.world.Container;
import net.minecraft.world.entity.player.Inventory;

public class WidgetInventory extends CompoundWidget {
    public WidgetInventory(int x, int y, int slotWidth, int slotHeight, int slotSeparation, Container container){
        int len = container.getContainerSize();
    }
}
